HashMap源码分析讲解
1：hash算法介绍
       算列表，又叫哈希表，它是基于快速存取的角度设计的，也是一个典型的空间换时间的做法。顾名思义，
   该数据可以理解为一个线性表，但是其中的元素不是紧密排列的，而是可能存在空隙。
      算列表（Hash table）也叫哈希表，它是根据关键码值（key->value）而直接进行访问的数据结构。也
   就是说，它通过关键码值映射到表中的某个位置来访问记录，以加快查找的速度。这个映射函数叫做散列函数
   存放记录的数字叫散列表。
       我们基于一种结果尽可能随机平均分布的固定函数H为每个元素安排位置，这样就可能避免遍历性质的线性
   搜索，以到达快速存取。但是由于随机性，也是必然导致一个问题就是冲突。所谓冲突，就是两个元素通过散列
   函数H得到相同的地址，那么这两个元素为同义词。
2：Hashmap 源码分析